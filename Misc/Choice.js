function Choice(That) {
  function Sow() {
    that = {
	  me: (Me),
	  Us: 'KarmaOp',
	  on: That.on,
	  Up: 'Map',
	  in: Map.boundingCoords,
	  as: That.as,
	  of: That.of,
	  my: Hex(That.on)
	};
//	call: unknown
  }

  return Sow;

}